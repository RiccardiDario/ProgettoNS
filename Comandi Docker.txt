Con questo comando vengono scaricate le immagini se non possedute sul sistema; 
vengono creati i conteiner e avviati. Nel caso in cui i conteiner già fossero
creati il comando avvia solo i conteiner
docker compose up -d 

I conteiner vengono stoppati e cancellati ( non vengono toccate le immagini)
docker compose down

Vengono stoppati i conteiner
docker compose stop

Mostra i conteiner attualmente nel sistema 
docker ps -a

Mostra le immagini nel sistema 
docker image ls -a

Mostra le network presenti nel tuo sistema (anche quelle di precedenti progetti. 
Ci sono tre network di defaul di docker che servono per il suo corretto funzionamento
e non devono essere cancellate)
docker network ls  

Elimina le network di conteiner non presenti più nel sistema
docker network prune

Elimina un specifico network con id pari a id_network
docker network rm id_network

iptables -L nei conteiner dei firewall mostra le regole

Per mailoney
Per il log andare a opt/mailoney
ip route add 172.1.1.0/24 via 172.1.3.2
ip route add 172.1.2.0/24 via 172.1.3.7
apk update && apk add iproute2

Per mailoney lanciare i seguenti comandi
telnet 172.1.1.4 25
EHLO example.com
MAIL FROM: <from@example.com>
RCPT TO: <to@example.com>

 

N.B Per Dionaea bisogna aggiungere prima la rotta
apt-get update && apt-get -y install  iproute2
ip route add 172.1.1.0/24 via 172.1.3.2
ip route add 172.1.2.0/24 via 172.1.3.7

Per interrogare sip su dionaea
echo -e "INVITE sip:172.1.3.5 SIP/2.0\r\n\r\n" | nc -w 1 172.1.3.5 5060
Per sip viene utilizzato udp, ma dionaea lo implementa in tcp
Non si ha risposta. Bisogna controllare il log per vedere l'interazione
I log di dionaea sono presenti al seguente path
opt/dionaea/var/lib/dionaea/bistreams/

Per testare il dns dal host interno o quello della dmz
nslookup  hostdmz.example.local
nslookup  webserver.example.local
nslookup  dionaea.example.local
